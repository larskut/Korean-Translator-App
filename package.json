{
  "name": "korean-translator-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nodemon --watch server --exec \"node server/index.js\"",
    "start": "node server/index.js",
    "build": "cp -r public/* ."
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "openai": "^4.69.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}

